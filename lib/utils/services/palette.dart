import 'package:flutter/material.dart';

// Applications Palette
class Palette {

  // app background color
  static const Color appBackground = Color.fromRGBO(232, 238, 240, 1);

  // selectedDrawer color
  static const Color selectedDrawerTabColor = Color.fromRGBO(36, 168, 185, 1);

  // appBar color
  static const Color appBarColor = Color.fromRGBO(0, 188, 212, 1);

  static const Color buttonBackgroundColor = appBarColor;

  static const Color circularProgress = appBarColor;

  // selectedTabBar color
  static const Color selectedTabBar = Color.fromRGBO(28, 150, 165, 1);

  // expenseTabBar color
  static const Color expenseTabBar = Color.fromRGBO(52, 141, 133, 1);

  //expenseTabBar color
  static const Color selectedExpenseTabBar = Color.fromRGBO(93, 164, 157, 1);

  //expenseTextFieldBorder color
  static const Color expenseTextFieldBorder = Color.fromRGBO(244, 67, 157, 1);

  //tableHeader color
  static const Color tableHeader = Color.fromRGBO(203, 203, 203, 1);

}